# Pick MA Job

## Next Version:

1. Review /configs folder and make sure that everything in there is made for any user that’ll use the platform. Ask for more input here on vision
2. User must be able to edit existing search configs. Because currently if you created config - you can’t change it
3. User should be able to pick multiple result rows and delete them. Because right now user can either delete all filtered rows or delete one by one which is inconvinient. Ask for more details on how I see it
4. User must be able to import existing config to the one they about to create. Ask for more input here on how I see it
5. Add search field so that user can find job by typing keywords.
6. When pipeline is ready to add jobs to result page - new list must be created. List must be called with the following format: platform_name-date_scraped-time_scraped. Newly scraped jobs must be automatically added to this list. The idea is that when pipeline is finished - new jobs can be lost because there could be just too many results and user can simply be lost in this. Ask for more input at this point to reflect on my vision for better results.