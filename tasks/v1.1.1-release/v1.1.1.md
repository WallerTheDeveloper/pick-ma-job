# Pick MA Job

## Next Version:

1. When user tries to add new config from the existing one - they get **Failed to execute 'text' on 'Response': body stream already read** toast at the bottom right of the screen. The error in dom console is following:
    1. 
    
    ajaxRequestInterceptor.ps.js:1
    POST [http://localhost:5173/api/search-configs](http://localhost:5173/api/search-configs) 500 (Internal Server Error)
    n	@	ajaxRequestInterceptor.ps.js:1
    fetch	@	ajaxRequestInterceptor.ps.js:1
    await in fetch		
    api	@	client.ts:59
    createSearchConfig	@	search-config.ts:22
    await in execute		
    handleSubmit	@	search-config.tsx:218
    <form>		
    PlatformTab	@	search-config.tsx:243
    <PlatformTab>		
    SearchConfigPage	@	search-config.tsx:422
    <SearchConfigPage>		
    App	@	App.tsx:40
    <App>		
    (anonymous)	@	main.tsx:13
    
    Payload is following 
    
    **Request URL**http://localhost:5173/api/search-configs**Request Method**POST**Status Code**500 Internal Server Error**Remote Address**[::1]:5173**Referrer Policy**strict-origin-when-cross-origin
    
    1. {platform: "upwork", query: "Unity",…}
        1. filters: {experienceLevel: ["intermediate", "expert", "entry"], jobType: ["fixed", "hourly"],…}
            1. experienceLevel: ["intermediate", "expert", "entry"]
                1. 0: "intermediate"
                2. 1: "expert"
                3. 2: "entry"
            2. jobType: ["fixed", "hourly"]
                1. 0: "fixed"
                2. 1: "hourly"
            3. maxJobAge: {value: 24, unit: "hours"}
                1. unit: "hours"
                2. value: 24
            4. paymentVerified: true
            5. perPage: 50
            6. sort: "newest"
        2. platform: "upwork"
        3. query: "Unity"
2. Add functionality to filter results rows by date added
3. Checkboxes near result rows look a bit like they are html default checkboxes. This makes them visually inappealing. Also checkboxes are not centered relative to the row but instead they are positioned at the top relative to result row. Make checkboxes visually appealing and also center them relative to result row. 
4. When user clicks on "select all visible" checkbox the panel appears which makes all result rows to get down a bit. This is visually inappealing. It's also a bad UX. If you need more input at this point - ask me
## Bugs:

1. When new list with freshly scraped jobs is created - the time doesn’t match the actual time of the user