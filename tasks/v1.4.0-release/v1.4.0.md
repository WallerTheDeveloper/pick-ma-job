# v1.4.0

1. Integrate writing upwork proposal for a picked job based on job description and profile
2. Add copy button to copy spefic text in cv summary window
3. “Job score is below evaluation threshold” feature must be removed, because user must be able to evaluate any job if they like
4. Change icon of the website in the tab
    
    ![ChatGPT Image May 7, 2026, 04_43_47 PM.png](ChatGPT_Image_May_7_2026_04_43_47_PM.png)
    
5. When user gets to profile page - the panel to the right (with pages names, version etc) has version, name, sign out and dark/light mode buttons too far beneath. It means that if user say wants to click on any of buttons or see info must scroll of the way down. The same goes for results page. Following UX is not the case for dashboard and search config pages so it means that left panel is glued to the lenght of right component (where the actual requested page info is). The correct UX must be that left panel (with version, sign out button etc) must not be dependent to the lenght of the rigth panel so that all of the bottom buttons and info are always visible to user
6. On the dashboard page user must be able to click on any of the pipeline tabs and see information on them. Because currently when user gets to other page and then returns/refreshes page - the infomation is gone and there is no way user can check it.
7. Add name of the company tag to result row so that user can filter out results by company name too
8. User is able to add skills with the same name but with different letter size. For example user is able to add “OpenCode”, “Opencode”, “openCoDE” etc. This should not be ignored. The correct scenario must be following (please note that variable names are examples and might not be named exactly like that. It is your job as AI developer to choose the most appropriate variable name in this case):
    1. User adds new skill called “OpenCode”
    2. User adds another new skill (likely by accident) “opencode”
    3. When this happens - user must be prompted with window saying “You’ve already added skills name_of_the_skill in concrete_field_where_added. Are you sure you want to add another skill with name skill_that_user_just_wanted_to_add_now”
9. When user adds skill in profile - it checks whether the skill was added and if so - platform restricts from adding this skill. But user doesn’t get any toast/notification about it which can make it confussing for some. Add toast that informs user that skill was already added. Also currently when for example skill wasn’t added in primary skill but was added to secondary skills and user tried to add this skill again in primary skills - then it’s added successfully. This should not be the case. It must be checked whether this skill was added to any of the field - and is so - it must not be added and user must be informed where it was added
10. Make tags in profile page movable. So that user can drag & drop tags with mouse. By tags I mean for example tags in skills section
11. Add button to stop pipeline
12. User must be able to bulk add to list by choosing multiple rows
13. If user chooses multiple rows and then goes to another list - selection must be canceled. User can bulk choose only in single list