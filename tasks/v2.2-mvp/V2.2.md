# Pick MA Job

## Features prio v2.2:

1. Everytime I go to main page session is not saved meaning I have to sign-in everytime I get on main page. We need a session token
2. Add clean button to clean all info from the profile
3. Website must have version number somewhere so that developer knows what version is currently deployed
4. Add a new scraper - LinkedIn. User must decide how many scrapers per pipeline run they want to use. If they want to use both, that must be possible. If they want to use only one of them - it must also be possible. Ask for more input from here on how I see it so we can reflect on this together and reflect on use cases.

## UX v2.2

1. When user runs pipeline - they see running status but when the pipeline is finished - the user still see pending status icon. This is a bad UX. It must work in the way that when status changes - it reflects change immediately for user without need to reload page. Another thing is that when user runs pipeline and then reloads the page before pipeline is finished - they see pending status but not animated “running” state.
2. User must be able to create a new list when clicking on “add to list” button from dropdown menu. Not just from the plus button on the top left corner of results page

## Plan for another versions

1. Integrate Buy me a coffee button
2. Add scheduling to scrape jobs every X time (post linkedIn scraper integration)
3. If scraped job is ≥7 then send notification to email
4. Integrate job specific resume adjustment based on job description and user’s profile
5. Integrate writing upwork proposal for a picked job based on job description and profile
6. Integrate cover letter generation for the found job. Create pdf (?)
7. Create multiple profiles for different searches. Pick only one to use for pipeline run. Example: user has experience in game development and full stack development
He must be able to:
    - Create multiple profiles (in this example user targets to game dev and full stack so creates both game dev profile and full stack dev profiles)
    - Pick one of the profiles
    - And based on picked profile - pipeline searches the job
    
    In other words we just want to add multiple profiles so that users can search different jobs without the need to clear profile everytime they want to target different jobs
    
8. Let admin have option to delete or add users. If user is added - email them with invitation mail.