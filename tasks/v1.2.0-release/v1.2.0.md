# Pick MA Job

v1.2.0:

1. Bug: Scraper failed for 'linkedin': Input is not valid: Field input.workType.1 must be equal to one of the allowed values: "on-site", "remote", "hybrid"
    1. Scenario: click on “Run Pipeline” button → wait for the pipeline to finish → when pipeline is finished see red text marking the error
    2. Notes: user can see scraped and evaluated jobs, but still sees this text after pipeline is finished. It is unclear what exactly is wrong and how it affects the pipeline
2. Feature: Integrate job specific resume adjustment based on job description and user’s profile
    1. User must be able to import CV as pdf file. AI must read and analyze it. After analysis and if job scored >= 7 point then user should have a button "Customize CV based on job needs" near result row. When user clicks on button - AI starts to analyze job description and suggest CV changes. The change must be in form of text. Format must be the same as in CV. The idea of feature is to increase user’s chances to pass ATS filtering and get interview invitation. It is allowed to exaggerate user’s skills for the sake of getting higher chances to get interview.
3. UX Feature: When clicking on add to list button and then clicking on list I want to add job in - dropdown list is hiding everytime i click. The user must to be able to close list when they need because they could want to add job to a different lists which forces them to click on the “add to list” button multiple times to open dropdown. This is a bad UX
    1. Scenario: click on “add to list” button on the result row → click on any list that user wants to add row → see that list is automatically closed
    2. Desired behaviour: click on “add to list” button on the result row → click on any list that user wants to add row → dropdown list is still not closed → click on area out of dropdown list → see that dropdown list is closed 
4. Bug: Filters do not work in lists. I can only filter in “all results” page. Lookup by keyword works
    1. Scenario: Go to any custom created list → try any filter → see bug that filtering doesn’t do anything
    2. Notes: when user uses lookup by keywords - it gives the desired results meaning that filtering by keywords works, but any other filtering don’t
5. UX Feature: Add button that mark bulk of rows as a desired status. User must be able to mark multiple result rows in one go in status that they want instead of marking every row one by one which is a bad UX
    1. Scenario: go to any list in “Results” page → select multiple result rows → click on “Mark as..” button and choose status that rows must be changed to
    2. Notes: this feature must be discussed with AI agent to design a better UX and user flow