export function SearchConfigPage() {
  return (
    <div>
      <h2 className="text-2xl font-semibold">Search Configuration</h2>
      <p className="mt-2 text-muted-foreground">Search config page — coming in Phase 6.</p>
    </div>
  );
}
